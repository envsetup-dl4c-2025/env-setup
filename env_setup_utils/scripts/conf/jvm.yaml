defaults:
  - defaults
  - _self_

tag: oss/jvm

inference:
  agent:
    agent_type: jvm
  data_source:
    hf:
      split: jvm_baseline_failure
  docker:
    image: ghcr.io/envsetup-dl4c-2025/envsetup-jvm:latest

evaluation:
  language: jvm
